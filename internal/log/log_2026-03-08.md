last updated: 2026-03-08

# Session Log: 2026-03-08

## Context
Continuing MLX optimization work after porting from PyTorch/CUDA. Previous session applied P1/P3/P4/P5/P6 from the validation plan (commit 5259929).

## Done

### bench.py profiling script (commit 8acb318)
- Created `bench.py` to instrument per-phase training timings
- Refactored `train.py` with `__main__` guard so model/config can be imported without side effects
- `build_model_config(depth, vocab_size)` now takes vocab_size as parameter

### Profiling results
| Phase | Avg time | % of step |
|-------|----------|-----------|
| fwd+bwd | 1657ms | 98.1% |
| data load | 20ms | 1.2% |
| optimizer | 13ms | 0.8% |

- 38.8K tok/sec steady state
- 53.5GB peak memory (of 192GB available)
- Eval: 642ms/step, 320 steps = 205s projected
- Caches verified working (mask: 1 entry, norm: 3 entries)

### Revised optimization priorities
1. **mx.compile on fwd+bwd** -- 98% of time, expected 10-30% speedup
2. **Eval speedup** -- 205s eval is too long; mx.compile on inference (no LR blocker)
3. **Muon optimizer** -- quality not speed
4. **Batch 64 investigation** -- memory headroom exists
5. ~~Async data loading~~ -- skip (1.2%)

## Done (continued)

### mx.compile on training step (commit fc279dd)
- Two-phase training: 11 uncompiled warmup steps, then compiled with state tracking
- Converted time-based LR schedule to step-based using optim.join_schedules
- Result: 1618ms -> 1408ms/step (15% speedup, 40.5K -> 46.5K tok/sec)
- Eval batch=64 tested: no speedup (compute-bound, not batch overhead)
- Compiled eval: ~7% speedup (555ms vs 594ms per step)

### Project infrastructure
- Created `log_utils.py` logging framework (--debug flag, AUTORESEARCH_DEBUG env var)
- Ported `analysis.ipynb` to `analysis.py` (outputs to data/ directory)
- Moved tests to `tests/` directory
- Created comprehensive AGENTS.md with project structure, optimization state, MLX notes
- Updated pyproject.toml to v0.3.0

### Muon optimizer via MultiOptimizer (v0.4.0)
- Root cause of MultiOptimizer KeyError: MLX's `tree_unflatten` converts string-digit dict keys ("0", "1") back to lists. When the optimizer's `_split_dictionary` calls `tree_unflatten`, the resulting list nodes cause `tree_map` to try integer indexing against dict state, producing `KeyError: 0`.
- Fix: use non-digit prefixed keys -- `self.layers = {"l0": ..., "l1": ...}` and `self.value_embeds = {"v0": ..., "v1": ...}`.
- Also moved `window_sizes` from `nn.Module` attribute to `GPTConfig.compute_window_sizes()` method -- plain Python lists stored as module attributes pollute the parameter/gradient tree.
- MultiOptimizer with filter-based routing: `is_muon_param(path, weight)` sends 2D+ layer weights (excluding ve_gate) to Muon, everything else to AdamW.
- Separate LR schedules per optimizer group (MATRIX_LR for Muon, EMBEDDING_LR for AdamW).
- Compiled eval for final evaluation pass.
- Fixed warmup step time estimation: skip first 4 steps instead of 2.
- All 8 tests pass (including 2 new MultiOptimizer tests).

### Per-param LR groups (v0.5.0)
- Expanded MultiOptimizer from 2 groups to 5, matching the baseline PyTorch per-param LR tuning:
  1. Muon (matrix weights, MATRIX_LR=0.04)
  2. AdamW (embeddings: wte + value_embeds, EMBEDDING_LR * dmodel_scale)
  3. AdamW (x0_lambdas, SCALAR_LR * dmodel_scale, beta1=0.96)
  4. AdamW (resid_lambdas, SCALAR_LR * 0.01 * dmodel_scale)
  5. AdamW fallback (lm_head + ve_gate, UNEMBEDDING_LR * dmodel_scale)
- Added `dmodel_scale = (n_embd / 768)^-0.5`. At depth=8 (n_embd=512), scale=1.2247.
- Added JSON result persistence to `data/run_*.json` and `data/bench_*.json` using orjson.
- All 8 tests pass including updated 5-group MultiOptimizer tests.

### v0.5.0 training results
| Metric | v0.5.0 (5-group) | v0.3.0 baseline |
|--------|-----------------|-----------------|
| val_bpb | **1.8999** | ~2.28 |
| avg_tok_sec | 33,423 | ~46,500 |
| peak_memory | 63.3GB | ~53GB |
| total_steps | 154 | ~189 |
| training_time | 302s | ~300s |

**val_bpb improved significantly** (1.90 vs 2.28). However:
- **Throughput regression**: Steady ~39K tok/sec for first 104 steps, then sudden drop to ~21K tok/sec (nearly 2x slowdown). Average fell to 33K. This needs investigation -- likely memory pressure from 5 optimizer states (63GB peak vs 53GB), thermal throttling, or MLX overhead with more optimizer groups.
- **Fewer steps**: Only 154 steps vs expected ~189 due to slower second half.
- **Eval very slow**: 440s for eval (total 741s), much longer than expected. Likely caused by memory pressure from optimizer state still resident during eval.

### Benchmark results (v0.5.0, bench_compare.py ours-only)
| Config | fwd+bwd ms | fwd+bwd tok/s | full step ms | full tok/s | peak MB |
|--------|-----------|---------------|-------------|-----------|---------|
| D=4 B=16 | 205.9 | 159,154 | 212.3 | 154,381 | 10,884 |
| D=8 B=32 | 1578.1 | 41,530 | 1721.5 | 38,074 | 53,542 |

Benchmark throughput consistent with previous versions (bench uses simple AdamW, not 5-group).

### Data investigations started
- Created `internal/data-investigations.md` with backlog of 6 data quality levers
- Data source is `karpathy/climbmix-400b-shuffle` (553M rows, single `text` column, no quality scores)
- No quality filtering in current pipeline -- all text used as-is
- prepare.py is READ ONLY, so filtering would need a separate preprocessing step

### Code cleanup and stabilization (v0.5.0 continued)
- Removed 3 dead schedule functions: `get_lr_multiplier`, `get_muon_momentum`, `get_weight_decay`
- Updated `bench.py` from 2-group to 5-group MultiOptimizer (both uncompiled and compiled sections)
- Updated `bench_compare.py` from plain AdamW to 5-group MultiOptimizer for fair comparison
- Updated `TestCompiledTrainingStep` to use 5-group MultiOptimizer (was still plain AdamW)
- Committed `bench_compare.py` (was untracked)
- Bumped `pyproject.toml` to 0.5.0

### Optimizer phase transition fix
- **Before**: Phase 2 created entirely fresh optimizer instances and hacked `opt._state['step']` on all 5 sub-optimizers to align step counters. This discarded Adam momentum buffers from warmup.
- **After**: Swap LR schedules into existing optimizer instances via `opt._schedulers['learning_rate'] = schedule`. Step counters naturally correct, momentum buffers preserved. Net: -48 lines, +7 lines.
- Key finding: `optimizer.learning_rate` setter only accepts scalars, but `_schedulers` dict accepts callables directly. MLX `_maybe_schedule()` checks `_schedulers` on each `update()` call.

### Structured JSON output format (v0.5.1 -> v0.5.2)
- Restructured `data/run_*.json` from flat dict to nested format with `format_version`, `hardware`, `model`, `training`, `result`, `data` blocks
- Added `step_timings` array to run JSON: each compiled-phase step records `(step, dt, tok_sec, loss)` plus `(active_mb, peak_mb)` every 10 steps
- `mx.reset_peak_memory()` at compiled-phase start so peak reflects only that phase
- Memory fields omitted from non-sampled steps via dict comprehension filtering `None` values
- Moved all JSON construction and serialization to `log_utils.py` per AGENTS.md convention:
  - `build_run_data(...)` -- constructs structured run JSON
  - `build_bench_data(configs)` -- constructs structured bench JSON
  - `save_json(prefix, data)` -- handles timestamp filename, orjson serialization
  - `hardware_info()` -- returns hardware metadata dict
- Removed inline `orjson`, `platform`, `os` imports from both `train.py` and `bench_compare.py`
- Updated `internal/data-investigations.md` to mark the proposed format as implemented

### Throughput regression analysis
- Wrote detailed analysis to `internal/analysis/2026-03-08_throughput-regression.md`
- 4 hypotheses ranked: memory fragmentation (high), thermal throttling (medium), graph recompilation (low), lazy optimizer init (low)
- Key insight: eval is also 2x slower (440s vs 205s expected), suggesting system-wide memory pressure rather than optimizer-specific overhead
- 5 diagnostic experiments proposed, 2 already implemented:
  - Memory sampling: `mx.get_active_memory()` + `mx.get_peak_memory()` every 10 steps in step_timings
  - Free optimizer before eval: full cleanup (`del optimizer, compiled_step`, all sub-optimizers, `gc.enable/collect`, `mx.clear_cache()`) before `evaluate_bpb()`
- 3 remaining experiments pending: 2-group baseline, fixed LR run, temperature monitoring

### v0.5.2 training results

| Metric | v0.5.2 | v0.5.0 | Delta |
|--------|--------|--------|-------|
| val_bpb | 1.886 | 1.900 | -0.7% |
| steps | 193 | 154 | +25% |
| avg_tok_sec | 41,931 | 33,423 | +25% |
| training_peak | 49.4GB | 63.3GB | -22% |
| total_seconds | 702.5 | 741 | -5% |
| throughput regression | NONE | step 104 | FIXED |

Key findings:
- Memory is flat (45.4GB active, 49.4GB peak) throughout all 193 steps -- rules out memory fragmentation hypothesis
- No throughput regression at all -- previous regression was likely caused by data cycling with only 3 shards (20 shards eliminates it) or was thermal/intermittent
- Eval still slow (~400s vs ~200s expected from bench.py) despite optimizer cleanup -- the 63GB overall peak is from eval batch=64, not training. The optimizer cleanup helped somewhat (741->702.5s) but eval is still the bottleneck
- More steps (193 vs 154) from sustained throughput is the main driver of val_bpb improvement

### v0.5.2 cleanup: simplify log_utils + training_peak_mb

- Removed `build_run_data` from log_utils.py (20-kwarg wrapper with zero abstraction value)
- Inlined the run_data dict construction in train.py
- Added `training_peak_mb` capture before optimizer cleanup -- separates training peak (49.4GB) from overall peak (63.3GB, dominated by eval at batch=64)
- Fixed data-investigations.md schema examples: replaced realistic-looking fake numbers with type placeholders
- Updated AGENTS.md hardware section with corrected throughput numbers

### Eval bottleneck analysis
- Root cause identified: memory environment, not algorithm. bench.py projects ~200s on a fresh model (~10GB active). train.py runs eval after 193 steps of allocation churn at 49GB active, then batch=64 pushes to 63GB peak. The 14GB surge in a fragmented Metal allocator pool causes 2x slowdown.
- Optimizer cleanup (v0.5.2) saved only ~38s (440->401s) -- confirms fragmentation is the dominant factor, not optimizer memory.
- Proposed fix: reduce EVAL_BATCH_SIZE from 64 to 32 (no throughput benefit from batch=64 per bench.py). Alternative: serialize model weights to numpy, reset allocator, reload.
- Quick test plan designed (~3 min, no full training needed): measure eval speed on clean vs fragmented memory to confirm hypothesis.
- Full analysis: internal/analysis/2026-03-08_eval-bottleneck.md

### prepare.py conformance analysis
- Line-by-line comparison of our MLX prepare.py against coderef/autoresearch/prepare.py (PyTorch/CUDA)
- All differences are mechanical API translations (torch -> mlx/numpy): .view -> .reshape, CUDA device -> unified memory, torch.no_grad -> noop, bool auto-promotion -> explicit float32 cast
- evaluate_bpb is numerically equivalent -- same masked sums, same float32 precision, same nats-to-bits conversion
- Data packing algorithm (best-fit BOS-aligned) is character-for-character identical
- Full analysis: internal/analysis/2026-03-08_prepare-py-conformance.md

## In Progress
- Data quality investigation #1: investigated, deprioritized (climbmix quality is high)

## Decisions
- Skipping async data loading entirely -- profiling showed only 1.2% of step time
- Reordered priorities based on actual profiling data rather than estimates
- Used "l" prefix for layer keys, "v" prefix for value embed keys to avoid tree_unflatten list conversion
- window_sizes computed on every forward pass (negligible cost, avoids parameter tree pollution)
- Throughput regression: resolved by increasing data shards from 3 to 20. Root cause was data cycling, not memory pressure or thermal throttling.

## Open Questions
- ~~mx.compile + time-based LR schedule~~ -- resolved: step-based schedule via optim.join_schedules
- Batch 64 crash: fragmentation or true OOM? (53GB at batch=32, should fit at 64)
- ~~Muon MultiOptimizer: KeyError on integer keys~~ -- resolved: non-digit dict keys
- ~~Muon val_bpb vs AdamW-only baseline~~ -- resolved: 5-group achieves 1.90 vs 2.28
- ~~Throughput regression at step ~104~~ -- resolved: 20 shards eliminates regression. Root cause was data cycling with 3 shards, not memory pressure.
- ~~Can we free optimizer state before eval?~~ -- done. Helps somewhat (741->702.5s) but eval is still ~400s.
- **Eval slowdown**: not fully explained by optimizer memory. Eval peak is 63GB (batch=64 is 2x training batch=32). Try eval at batch=32 or free model caches before eval.
- **climbmix has no quality metadata**: unlike FineWeb-Edu, there are no quality scores in the parquet files. Quality filtering would require computing scores ourselves (e.g. perplexity from a reference model, text length heuristics, language detection).
