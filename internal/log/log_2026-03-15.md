# Session Log: 2026-03-15

## Upstream sync from karpathy/autoresearch

### What changed
Compared our MLX port against the upstream karpathy/autoresearch codebase (local reference at `coderef/autoresearch-karpathy`) and synced 4 algorithmic improvements:

1. **`estimate_flops` method** -- upstream properly excludes embedding/scalar params from base FLOPs and adds per-window attention FLOPs. Our naive `6 * num_params` was overestimating by ~32%.

2. **Muon momentum schedule** -- upstream ramps momentum from 0.85 to 0.95 over 300 steps. We had it fixed at 0.95 from step 0. Added via `_schedulers['momentum']` on the Muon optimizer.

3. **Weight decay schedule** -- upstream linearly decays weight decay to 0 over training. We had constant WD. Added via `_schedulers['weight_decay']` on the Muon optimizer.

4. **NaN check** -- upstream checks `math.isnan(loss) or loss > 100`. We only had `> 100`.

### What was intentionally skipped
- CUDA-specific: FA3, torch.compile, pin_memory, device transfers
- Hardware-specific defaults: DEPTH=8, DEVICE_BATCH_SIZE=128, TOTAL_BATCH_SIZE=2^19 (all justified for H100 but wrong for Apple Silicon)
- H100 MFU calculation (989.5 TFLOPS reference)
- PR #2 (FA3 non-Hopper fallback) -- entirely CUDA-specific

### program.md rewrite
Rewrote program.md starting from upstream's latest version, adapted for our context:
- Added "Apple Silicon notes" section with throughput, MLX, unified memory, and batch size guidance
- Updated example output numbers to match our actual v0.6.1 results
- Added CLAUDE.md to the "read these files" list
- Adjusted timeout guidance (60-90s overhead on Apple Silicon vs "a few seconds" on GPU)
- Removed GPU-specific references (single-GPU, VRAM, gpu0 branch naming)

### Key technical finding
The `estimate_flops` method reveals our old FLOPs estimate was inflated by ~32% because it counted all parameters (including embeddings and scalars) in the `6*N` formula. The new estimate correctly only counts matrix FLOPs from transformer layers + lm_head, plus attention FLOPs per window size.

### Open questions
- Do the momentum/WD scheduler changes actually improve val_bpb? Need a training run to verify. The schedules are set via `_schedulers` dict on the MLX Muon optimizer -- should work but hasn't been validated at runtime yet.
- If compiled training (grad_accum=1) is used, do the momentum/WD schedules cause recompilation? Learning rate schedules work because LR is likely stored as mx.array, but momentum/WD may be Python floats.

## Unified throughput+quality evaluation and docs update

### What changed
Expanded the experiment loop to treat throughput as a first-class signal alongside val_bpb, and updated all documentation to reflect v0.7.0 changes.

1. **program.md**: Replaced binary val_bpb keep/discard (steps 8-9) with a quality+throughput decision framework. Agent now considers avg_tok_sec alongside val_bpb when deciding whether to keep or discard changes. Added guidance thresholds (not hard rules): keep if throughput improves >10% with neutral bpb, discard if throughput craters >15% for marginal bpb gain. Also added bench.py to setup steps and updated results.tsv to 6 columns (added avg_tok_sec).

2. **analysis.py**: Added avg_tok_sec parsing in load_results(), throughput display in print_summary() for kept experiments, and a second subplot in plot_progress() showing throughput over experiment iterations.

3. **CLAUDE.md**: Updated to v0.7.0 -- results.tsv schema now 6 columns, Current State reflects momentum/WD schedules, added cross-reference to ANE integration roadmap.

4. **AGENTS.md**: Fixed stale Known Deviations (momentum/WD are now synced with upstream, only dmodel_scale deviation remains). Updated Muon routing table to show momentum ramp. Updated Agent Feedback Loop to document 6-column results.tsv and decision framework. Added Future Work section pointing to ANE roadmap.

5. **internal/ane-integration.md**: New file. Phased roadmap for ANE integration as a multi-session engineering effort. Phase 0 is a go/no-go benchmark gate -- if ANE effective throughput doesn't beat current GPU throughput, stop. Phases 1-4 cover bridge validation, single-layer port, backward pass, and training loop integration. Documents key constraints (FP16 only, channel-first layout, ~119 compile limit, causal masking).

### Key decisions
- Throughput thresholds in program.md are guidance, not hard rules. The agent uses judgment, same as the existing simplicity criterion.
- avg_tok_sec varies ~3-5% between identical runs (Metal scheduling, thermals), so marginal cases should be re-run.
- ANE integration is tracked separately from experiments -- it's infrastructure work, not a train.py experiment.
- No migration needed for results.tsv -- it's gitignored and recreated per experiment branch.

### Open questions
- Should analysis.py compute a composite score (bpb + throughput) for ranking experiments, or keep them as separate signals?
- ANE Phase 0 needs to be run to determine viability. No point planning Phases 1-4 in detail until the benchmark results are in.

## Data experiment program and documentation restructure

### What changed
Added a data experiment autonomous loop and restructured documentation for clarity.

1. **program_data.md** (new): Autonomous data experiment loop modeled on program.md. File scope: agent edits prepare.py + data_sources.py, runs train.py as-is. evaluate_bpb is locked. Same results.tsv format and keep/discard framework. Starting backlog includes Nemotron-inspired techniques (two-phase curriculum, quality-tiered mixing, synthetic data augmentation, dedup) and existing backlog items from data-investigations.md.

2. **docs/guide.md** (new): Step-by-step usage guide for all modes -- model experiments, data experiments, engineering, manual operations. Includes table of which data changes require re-running prepare.py. Moved "tuning for smaller machines" content here from README.

3. **README.md** (restructured): Added "what you can do" table linking to all three programs + guide. Condensed "what changed from original" to 4 bullets. Updated project structure to show program_data.md and docs/guide.md. Updated results to v0.7.0. Added Nemotron paper reference in documentation section.

4. **CLAUDE.md** (updated): Clarified file scope per experiment program in Constraints section. Added program_data.md and docs/guide.md to Project Structure. Updated Experiment Workflow to reference both programs.

5. **internal/data-investigations.md** (updated): Updated items 3 (curriculum) and 6 (tokenizer) to reflect they're now unblocked by the data program. Updated item 1 (filtering) to note prepare.py is now editable. Added items 8 (quality-tiered mixing) and 9 (synthetic data augmentation) from Nemotron paper.

6. **CHANGELOG.md**: Extended v0.7.0 with the 4 new items.

### Key decisions
- Model and data are parallel research tracks with the same eval metric. Engineering (ANE) is separate infrastructure work. This three-track framing is reflected in the README table and guide.
- program_data.md uses `autoresearch-data/<tag>` branch naming to distinguish from model experiments (`autoresearch/<tag>`).
- No code changes in this commit -- all documentation/program files only. Tests still pass.

### Open questions
- Should the data program have its own AGENTS.md equivalent for accumulated data engineering knowledge, or is data-investigations.md sufficient?
- ~~How to handle experiments that span both programs (e.g., token loss weighting needs train.py + prepare.py changes)?~~ Resolved: cross-program experiments are explicitly human-directed, not autonomous loop work. Documented in program_data.md backlog item 8.

## Post-implementation review fixes

### What changed
Reviewed committed program_data.md, CLAUDE.md, and docs/guide.md with critical eyes and fixed several issues.

1. **program_data.md bug fix**: `data-investigations.md` -> `internal/data-investigations.md` (agent would fail to find the file).

2. **program_data.md missing sections**: Added Memory paragraph (data changes can affect memory), Simplicity criterion (complex filtering for 0.001 bpb not worth it), and expanded NEVER STOP to match program.md's depth (overnight example, "think harder" guidance). Data experiments have a smaller design space, so the agent is more likely to feel stuck.

3. **program_data.md setup reads**: Added AGENTS.md to the file list with note pointing to "Data Pipeline" section (how prepare.py works -- numpy packing, text_iterator semantics).

4. **program_data.md backlog reorder**: Moved easy wins to top (1: more shards = zero code changes, 2: length filtering = simple text_iterator change, 3: tokenizer optimization = straightforward). Hard Nemotron techniques moved to "Data quality techniques" subsection. An autonomous agent starts from the top.

5. **program_data.md eval compatibility**: Added note to "What you CANNOT edit" about keeping validation shards loadable. Broken eval loader crashes are confusing, not obvious.

6. **program_data.md cross-program clarity**: Backlog item 8 (token weighting) now explicitly states cross-program experiments are human-directed manual work, not autonomous loop work.

7. **CLAUDE.md**: Updated opening description to mention both experiment programs (was "An AI agent iterates on train.py").

8. **.claude/settings.json** (new): Scoped allowlist for autonomous experiment runs. Covers file read/edit/write, uv run, git operations, shell utilities. Explicitly denies git push.

9. **docs/guide.md**: Added "Running with Claude Code" section with interactive mode (human approves tool calls), autonomous mode (pre-configured allowlist, -p flag, walk away), safety notes (experiment branch isolation, no push).

### Key decisions
- Allowlist is committed as `.claude/settings.json` (project-level, shared). Personal overrides go in `.claude/settings.local.json` (gitignored).
- `git push` explicitly denied in allowlist. Agent works locally only.
- `git reset` allowed because the experiment loop needs it for discarding failed experiments on dedicated branches.

### Open questions
- Should the autonomous launch command also set `--max-turns` to prevent runaway sessions, or is Ctrl+C sufficient?
